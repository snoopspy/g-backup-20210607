#include "gpdu.h"

// ----------------------------------------------------------------------------
// GPdu
// ----------------------------------------------------------------------------

// ----------------------------------------------------------------------------
// GPdus
// ----------------------------------------------------------------------------
GPdus::iterator GPdus::findIterator(GPdu::Flag flag) {
  for (GPdus::iterator it = begin(); it != end(); it++) {
    GPdu* pdu = *it;
    if (pdu->flag() == flag)
      return it;
  }
  return end();
}
